"""
Конфигурация Bybit DCA Trading Bot (Average Range Strategy)

Все настройки вынесены сюда. Меняйте параметры под свои нужды.
"""

# =====================================================================
# BYBIT API
# =====================================================================
BYBIT_API_KEY = ""          # Ваш API ключ Bybit
BYBIT_API_SECRET = ""       # Ваш API секрет
BYBIT_TESTNET = True        # True = тестнет, False = реальная торговля

# =====================================================================
# ТОРГОВАЯ ПАРА И ТАЙМФРЕЙМ
# =====================================================================
SYMBOL = "BTCUSDT"          # Торговая пара (можно менять: ETHUSDT, SOLUSDT и т.д.)
TIMEFRAME = "D"             # Таймфрейм свечей для графика и расчёта сигнала
SIGNAL_TIMEFRAME = "D"      # Таймфрейм для расчёта сигнала покупки (синхронизируется с TIMEFRAME)
LOOKBACK_PERIOD = 5         # Количество баров для расчёта среднего диапазона

# =====================================================================
# БАЛАНС И УПРАВЛЕНИЕ КАПИТАЛОМ
# =====================================================================
TOTAL_BALANCE = 1000.0      # Общий баланс на стратегию (USD)

# =====================================================================
# DCA-УСРЕДНЕНИЕ
# =====================================================================
# Каждая точка усреднения:
#   level       — номер уровня (1 = первый вход)
#   drop_pct    — % падения от цены ПЕРВОГО входа для срабатывания
#   balance_pct — % от TOTAL_BALANCE для этой покупки
#
# Пример: drop_pct=3.0 значит покупка сработает когда цена упадёт
#          на 3% от первоначальной цены входа.
#
# Сумма всех balance_pct не должна превышать 100%.
DCA_POINTS = [
    {"level": 1, "drop_pct": 0.0,  "balance_pct": 20.0, "tp_pct": 1.0},   # Первый вход (сигнал индикатора)
    {"level": 2, "drop_pct": 3.0,  "balance_pct": 15.0, "tp_pct": 1.0},   # -3% от первого входа
    {"level": 3, "drop_pct": 6.0,  "balance_pct": 20.0, "tp_pct": 0.8},   # -6% от первого входа
    {"level": 4, "drop_pct": 10.0, "balance_pct": 20.0, "tp_pct": 0.6},   # -10% от первого входа
    {"level": 5, "drop_pct": 15.0, "balance_pct": 25.0, "tp_pct": 0.5},   # -15% от первого входа
]

# =====================================================================
# ЛОГИРОВАНИЕ
# =====================================================================
LOG_FILE = "bot.log"        # Файл логов
LOG_LEVEL = "INFO"          # Уровень логирования (DEBUG, INFO, WARNING, ERROR)

# =====================================================================
# ИНТЕРВАЛ ПРОВЕРКИ
# =====================================================================
CHECK_INTERVAL_SECONDS = 60  # Как часто проверять рынок (секунды)
